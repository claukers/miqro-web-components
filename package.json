{
  "name": "@miqro/web-components",
  "version": "0.0.1",
  "description": "helpers for creating dynamic HTMLElements with a basic template system",
  "type": "commonjs",
  "types": "dist/cjs/index.d.ts",
  "main": "dist/cjs/index.js",
  "scripts": {
    "prepare": "npm run build",
    "prebuild": "rm -Rf dist/;",
    "build": "tsc && tsc -p tsconfig-esm.json",
    "pretest": "npm run build",
    "test": "LOG_LEVEL=debug NODE_ENV=test miqro-test test/*.test.js -n ",
    "predemo:serve": "npm run build && webpack",
    "demo:serve": "LOG_LEVEL=debug NODE_ENV=test DEMO_API_PORT=9696 node test/data/app/server/server.js",
    "demo:watch": "miqro watch test/data/app/front/src/ \"npm run predemo:serve\" & npm run demo:serve"
  },
  "author": "claukers",
  "license": "ISC",
  "devDependencies": {
    "@miqro/core": "^1.5.11",
    "@miqro/request": "^0.0.5",
    "@miqro/test": "^0.0.11",
    "miqro": "^1.7.11",
    "typescript": "^4.5.4",
    "webpack-cli": "^4.9.2"
  },
  "dependencies": {
    "@miqro/parser": "^0.0.5"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=6.0.0"
  }
}
