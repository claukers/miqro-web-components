{
  "name": "@miqro/web-components",
  "version": "0.0.1",
  "description": "helpers for creating dynamic HTMLElements with a basic template system",
  "type": "commonjs",
  "types": "dist/cjs/index.d.ts",
  "main": "dist/cjs/index.js",
  "scripts": {
    "prepare": "npm run build",
    "prebuild": "rm -Rf dist/;",
    "tsc": "npm run prebuild && tsc",
    "build": "tsc && tsc -p tsconfig-esm.json",
    "prebuild:sfc:example": "npm run build && npx miqro transpile:sfc -m \"./lib\" \"./examples/sfc/elements/\" \"./examples/sfc/src/\"",
    "build:sfc:example": "webpack",
    "pretest": "npm run build",
    "test": "LOG_LEVEL=debug NODE_ENV=test miqro-test -r src/ -i -n "
  },
  "author": "claukers",
  "license": "ISC",
  "devDependencies": {
    "webpack": "5.72.1",
    "webpack-cli": "^4.9.2",
    "@miqro/test": "0.2.2",
    "miqro": "../miqro",
    "typescript": "4.6.3"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=6.0.0"
  },
  "dependencies": {
  }
}
